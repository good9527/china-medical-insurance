# 全国医疗保险政策精算与多端测算平台标准化规程 (SOP)

为了确保全国 31 个省级行政区、300+ 统筹区的数据在长周期维护、自动化持续集成（CI/CD）、多端部署和政策动态修订过程中的绝对准确性、溯源可查性与代码规范性，特制定本标准化操作与更新指南。

---

## 一、 数据架构与模块化规范

每个省份在 `src/data/<province_pinyin>/` 建立独立目录，每个地级市设立独立的数据文件 `<city_pinyin>.ts`：

1. **强绑定官方红头文件溯源** (`sourceDocs`)：
   - 至少收录 2 份以上当地医疗保障局（`ybj.*.gov.cn`）或人民政府（`*.gov.cn`）官方颁布的规范性文件；
   - 必须包含真实官方发文字号（如“穗医保规字〔2022〕3号”、“晋医保发〔2024〕11号”）；
   - 必须附带官方原文永久或公开有效直链（`officialUrl`）；
   - 必须包含政策关键条款原文摘录（`summaryQuote`），杜绝二手估算。

2. **多层次精细化待遇建模**：
   - **门诊共济**：精确区分年累计起付线与次均起付线形态（如广东省普遍 0 元起付、山西省三类机构 30/50/80 元按次起付且 300 元年累计封顶等）；
   - **倾斜优待**：刻画退休人员报销比例上浮（`retireeRatioBonus`）与门诊封顶线倾斜（`annualCapRetiree`）；
   - **5级定点医疗机构**：`community`（基层社区）、`tier1`（一级）、`tier2`（二级）、`tier3`（三级）、`tier3_top`（三甲重点）；
   - **大病保险与大额互助**：梯度起付标准、分段报销比率、年度最高支付限额；
   - **异地就医**：长期居住备案、规范转诊备案、未备案急诊及自行就医降比系数。

---

## 二、 自动化验证与质量门禁 (Quality Gate)

项目已建立一键化质量检查命令：

```bash
# 1. 自动执行数据规范性、字段完整性与官方红头文件链接审计
npm run audit:data

# 2. 自动运行全国所有统筹区精算场景与资金守恒测试
npm run test:calc

# 3. 生产打包全编译校验 (H5 / 小程序端)
npm run build:h5

# 4. 一键执行全流水线检查 (合并上述三项)
npm run check:all
```

---

## 三、 新增省份 / 地级市标准化操作步骤

当扩展一个新省份或新地级市时，严格遵循以下 5 步流水线：

1. **法规政策检索与官方核实**：
   - 查询省人民政府、省医保局及市医保局最新公开的规范性红头文件。
2. **构建城市 TypeScript 模块**：
   - 在 `src/data/<province>/` 下建立 `<city>.ts`，实现类型 `CityInsuranceData`。
3. **注册省市联动与全量池**：
   - 在 `src/data/<province>/index.ts` 导出并追加至全省列表；
   - 在 `src/data/index.ts` 将全省列表汇入 `allCities`；
   - 在 `src/data/provinces.ts` 中将对应城市设置为 `hasData: true`。
4. **编写真实算例单元测试**：
   - 在 `src/engine/__tests__/calculator.test.ts` 中加入该城市的专项门诊、住院或异地就医测算断言。
5. **执行一键验证与构建**：
   - 运行 `npm run check:all` 确保 100% 通过且构建零报错。

---

## 四、 政策年度更新与修订流程

每年各地医保政策更新（如每年 7 月社平工资调整导致缴费基数、门诊限额及起付线微调）：

1. **定位对应城市数据文件**：直接在 `src/data/<province>/<city>.ts` 修改对应字段；
2. **追加新政策依据**：在 `sourceDocs` 数组中追加新文件，将上一年度旧文件状态置为 `superseded`，新文件置为 `active`；
3. **更新 `lastUpdated`** 字段为最新维护日期；
4. **运行 `npm run check:all`** 确保更新无回归风险并成功发布。
